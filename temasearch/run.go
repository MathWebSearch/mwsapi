package temasearch

// RunQuery runs a query
func RunQuery(connection *Connection, query *Query, from int64, size int64) (res *interface{}, err error) {
	return
}

// CountQuery counts the results of a query
func CountQuery(connection *Connection, query *Query) (count int64, err error) {
	return
}
